IDENTIFICATION DIVISION.
PROGRAM-ID. DATAFILES.

ENVIRONMENT DIVISION.
REPOSITORY.
    COPY "PARTITIONDATA.cpy".
    COPY "PARTITIONFIELD.cpy".
    COPY "PARTITIONSPEC.cpy".
    COPY "STRUCTLIKE.cpy".
    COPY "FILEFORMAT.cpy".
    COPY "METRICS.cpy".
    COPY "SORTORDER.cpy".
    COPY "ENCRYPTIONKEYMETADATA.cpy".

DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-PARTITION-DATA USAGE POINTER.
01 WS-PARTITION-PATH PIC X(256).
01 WS-PARTITION-VALUES OCCURS 32 TIMES.
   05 WS-PARTITION-VALUE PIC X(256).
01 WS-PARTITION-FIELD USAGE POINTER.
01 WS-PARTITION-SPEC USAGE POINTER.
01 WS-JAVA-CLASSES USAGE POINTER.
01 WS-PARTITION-DATA-REUSE USAGE POINTER.
01 WS-FILE-PATH PIC X(256).
01 WS-FILE-FORMAT PIC X(32).
01 WS-RECORD-COUNT PIC 9(18) COMP-3.
01 WS-FILE-SIZE-IN-BYTES PIC 9(18) COMP-3.
01 WS-COLUMN-SIZES USAGE POINTER.
01 WS-VALUE-COUNTS USAGE POINTER.
01 WS-NULL-VALUE-COUNTS USAGE POINTER.
01 WS-NAN-VALUE-COUNTS USAGE POINTER.
01 WS-LOWER-BOUNDS USAGE POINTER.
01 WS-UPPER-BOUNDS USAGE POINTER.
01 WS-KEY-METADATA PIC X(32766).
01 WS-SPLIT-OFFSETS USAGE POINTER.
01 WS-SORT-ORDER-ID PIC 9(9) COMP.

PROCEDURE DIVISION.

COPY "NEWPARTITIONDATA.cob".
COPY "COPYPARTITIONDATA.cob".
COPY "FILLFROMPATH.cob".
COPY "FILLFROMVALUES.cob".
COPY "DATADATA.cob".
COPY "COPY.cob".
COPY "FROMMANIFEST.cob".
COPY "BUILDER.cob".

STOP RUN.